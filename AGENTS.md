# AGENT 操作规范

## 目标
本文件用于约束 Agent 在本项目中的日常维护操作。

## 维护标准
每一次操作完成后，必须进行 Git 仓库更新与推送，最少包括以下步骤：

1. 检查变更状态：`git status`
2. 拉取远端最新代码并处理同步：`git pull`
3. 提交本次变更：`git add -A` + `git commit -m "<message>"`
4. 推送到远端：`git push`

## 说明
- 若存在冲突，先解决冲突并验证通过后再推送。
- 若本次无代码变更，需在任务记录中明确说明“无需提交/推送”。
